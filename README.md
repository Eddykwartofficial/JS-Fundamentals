// Create a constant variable named myVar and assign it the specified string value.
// The 'const' keyword ensures that the value of this variable cannot be changed later in the script.
const myVar = "JavaScript is amazing";

// Use console.log() to print the value of the myVar variable to the web console.
console.log(myVar);
